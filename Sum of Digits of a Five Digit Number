#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    
    int a;
    scanf("%d", &a);

    int sumn=0;
    while(a>0){
       sumn+=a%10;
       a/=10; 
    }
    printf("%d",sumn);
    return 0;
}
